{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for Patient Dashboard",
  "main": "Server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "postinstall": "cd ../public && npm install && npm run build"
  },
  "keywords": [],
  "author": "Jaswanth",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.3"
  },
  "engines": {
    "node": "18.x"
  }
}
